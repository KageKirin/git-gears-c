
query ($_fullPath: ID!, $_count: Int!)
{
	project(fullPath: $_fullPath)
	{
		id,
		name,
		webUrl,
		issues(first: $_count)
		{
			nodes
			{
				iid,
				title,
				description,
				webUrl,
			}
		}
	}
}
