
query($_owner: String!, $_name: String!, $_number: Int!)
{
	repository(owner: $_owner, name: $_name)
	{
		id,
		name,
		url,
		issue(number: $_number)
		{
			number,
			bodyText,
			state,
			title,
			url,
		}
	}
}
